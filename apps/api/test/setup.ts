import { beforeAll, afterAll, afterEach } from 'vitest';

// This file is for global test setup, like starting a test server or database.
// For now, it's empty, but it's good practice to have it.

beforeAll(() => {
  // Runs once before all tests
});

afterAll(() => {
  // Runs once after all tests
});

afterEach(() => {
  // Runs after each test, useful for cleaning up mocks
});
